org.gradle.jvmargs=-Xmx4096m -Xms1024m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# 应用版本配置 (1.5.3 - 同步全链路修复:同步 API ApiResponse 包装 + RDS api_users email→account + UUID snapshot 修复 + UI 显示账号而非 tenantID
APP_VERSION_CODE=24
APP_VERSION_NAME=1.5.3
