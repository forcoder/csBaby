# 云端同步功能测试用例 (QA)

## 基本信息
- **功能模块**: 云端同步
- **测试环境**: 华为 ALN-AL00 (HarmonyOS)
- **后端服务**: https://csbaby-sync-server.onrender.com


## 测试账号

---

## 补充测试用例

### TC-11: 多端并发与冲突处理
| 项目 | 内容 |
|------|------|
| 用例ID | TC-11 |
| 标题 | 多设备并发同步与冲突检测 |
| 前置条件 | 同一账号已在两台设备登录 |
| 测试步骤 | 1. A 端添加/修改规则<br>2. B 端离线后也修改同一规则<br>3. B 端上线并同步<br>4. 检查同步结果及冲突提示 |
| 预期结果 | 有冲突提示或自动合并，数据一致性符合设计预期 |
| 优先级 | P0 |

### TC-12: 增量同步与删除同步
| 项目 | 内容 |
|------|------|
| 用例ID | TC-12 |
| 标题 | 仅变更部分数据时的同步行为 |
| 前置条件 | 本地和云端均有数据 |
| 测试步骤 | 1. 本地仅修改部分规则<br>2. 点击同步<br>3. 删除本地部分规则后同步 |
| 预期结果 | 仅变更部分被同步，删除操作正确同步到云端，不发生全量覆盖 |
| 优先级 | P1 |

### TC-13: 数据回滚与历史版本恢复
| 项目 | 内容 |
|------|------|
| 用例ID | TC-13 |
| 标题 | 误操作后数据回滚能力 |
| 前置条件 | 已有多次同步历史 |
| 测试步骤 | 1. 删除/修改规则后同步<br>2. 检查是否支持回滚/恢复历史版本 |
| 预期结果 | 可恢复到历史版本或有回滚提示 |
| 优先级 | P2 |

### TC-14: 大数据量/性能边界测试
| 项目 | 内容 |
|------|------|
| 用例ID | TC-14 |
| 标题 | 大量数据同步性能与稳定性 |
| 前置条件 | 本地/云端有1000条以上规则 |
| 测试步骤 | 1. 批量导入大量规则<br>2. 触发同步<br>3. 观察同步时长、内存、是否分批 |
| 预期结果 | 同步不超时/崩溃，性能可接受，数据完整 |
| 优先级 | P2 |

### TC-15: 账号异常与安全场景
| 项目 | 内容 |
|------|------|
| 用例ID | TC-15 |
| 标题 | 账号被禁用/删除/异地登录时同步行为 |
| 前置条件 | 账号被后台禁用/删除或异地登录 |
| 测试步骤 | 1. 触发同步<br>2. 检查提示与安全处理 |
| 预期结果 | 正确提示账号异常，禁止同步，或强制退出登录 |
| 优先级 | P1 |

### TC-16: 断点续传与中断恢复
| 项目 | 内容 |
|------|------|
| 用例ID | TC-16 |
| 标题 | 同步中断后自动恢复 |
| 前置条件 | 正在同步过程中 |
| 测试步骤 | 1. 同步时断网/杀进程/崩溃<br>2. 重启 App 并重新同步 |
| 预期结果 | 能自动断点续传或重新同步，数据不丢失不重复 |
| 优先级 | P2 |

### TC-17: 数据完整性校验
| 项目 | 内容 |
|------|------|
| 用例ID | TC-17 |
| 标题 | 同步后本地与云端数据一致性 |
| 前置条件 | 完成一次完整同步 |
| 测试步骤 | 1. 同步完成后分别比对本地与云端数据（如 hash/条目数） |
| 预期结果 | 本地与云端数据完全一致，无丢失/重复/错乱 |
| 优先级 | P1 |

### TC-18: 异常数据与兼容性场景
| 项目 | 内容 |
|------|------|
| 用例ID | TC-18 |
| 标题 | 云端返回异常/兼容性数据处理 |
| 前置条件 | 云端返回字段缺失/异常结构/服务端升级降级 |
| 测试步骤 | 1. 通过接口注入异常数据结构<br>2. 触发同步 |
| 预期结果 | App 能容错处理，提示异常或自动兼容，不崩溃 |
| 优先级 | P2 |
| 字段 | 值 |
|------|-----|
| 邮箱 | test@test.com |
| 密码 | 123456 |
| TenantId | 00b04199-e6ed-4a1f-a03e-d1db40315f3b |

---

## 测试用例

### TC-01: 用户登录
| 项目 | 内容 |
|------|------|
| 用例ID | TC-01 |
| 标题 | 用户使用邮箱密码登录 |
| 前置条件 | 用户已注册 |
| 测试步骤 | 1. 打开 App<br>2. 进入"我的"页面<br>3. 输入邮箱: test@test.com<br>4. 输入密码: 123456<br>5. 点击"登录" |
| 预期结果 | 显示"登录成功"，自动触发数据同步 |
| 优先级 | P0 |

### TC-02: 用户注册
| 项目 | 内容 |
|------|------|
| 用例ID | TC-02 |
| 标题 | 新用户注册并自动同步 |
| 前置条件 | 无 |
| 测试步骤 | 1. 打开 App<br>2. 进入"我的"页面<br>3. 点击"注册"<br>4. 输入邮箱、密码、昵称<br>5. 点击"注册" |
| 预期结果 | 注册成功并登录，自动触发首次全量同步 |
| 优先级 | P0 |

### TC-03: 首次登录全量同步
| 项目 | 内容 |
|------|------|
| 用例ID | TC-03 |
| 标题 | 登录后自动同步云端数据到本地 |
| 前置条件 | 云端有数据（通过其他设备上传） |
| 测试步骤 | 1. 确保云端有测试数据<br>2. 登录账号<br>3. 等待同步完成 |
| 预期结果 | 云端数据下载到本地，知识库显示云端规则 |
| 优先级 | P1 |

### TC-04: 手动触发立即同步
| 项目 | 内容 |
|------|------|
| 用例ID | TC-04 |
| 标题 | 用户手动点击立即同步 |
| 前置条件 | 用户已登录，有本地数据 |
| 测试步骤 | 1. 登录账号<br>2. 进入"我的"页面<br>3. 点击"立即同步"按钮 |
| 预期结果 | 显示"同步完成"，本地数据上传到云端 |
| 优先级 | P0 |

### TC-05: 本地数据上传到云端
| 项目 | 内容 |
|------|------|
| 用例ID | TC-05 |
| 标题 | 验证本地知识库规则同步到云端 |
| 前置条件 | 登录账号，本地有知识库规则 |
| 测试步骤 | 1. 在知识库添加一条规则<br>2. 点击"立即同步"<br>3. 同步完成后用 API 查询云端数据 |
| 验证方法 | `GET https://csbaby-sync-server.onrender.com/sync/all?tenantId=xxx`<br>检查 keywordRules 数组 |
| 预期结果 | 云端返回数据包含本地添加的规则 |
| 优先级 | P1 |

### TC-06: 卸载重装后数据恢复
| 项目 | 内容 |
|------|------|
| 用例ID | TC-06 |
| 标题 | 卸载 App 重新安装后恢复云端数据 |
| 前置条件 | 账号已同步过数据到云端 |
| 测试步骤 | 1. 记录当前数据<br>2. 卸载 App<br>3. 重新安装<br>4. 登录相同账号 |
| 预期结果 | 云端数据恢复到本地 |
| 优先级 | P1 |

### TC-07: Token 过期处理
| 项目 | 内容 |
|------|------|
| 用例ID | TC-07 |
| 标题 | Token 过期后自动刷新或重新登录 |
| 前置条件 | 登录后等待 Token 过期（或模拟） |
| 测试步骤 | 1. 登录账号<br>2. 等待 Token 过期（约7天）<br>3. 点击同步 |
| 预期结果 | 自动使用 refreshToken 刷新，或提示重新登录 |
| 优先级 | P2 |

### TC-08: 同步状态显示
| 项目 | 内容 |
|------|------|
| 用例ID | TC-08 |
| 标题 | 同步过程中显示正确状态 |
| 前置条件 | 用户已登录 |
| 测试步骤 | 1. 点击"立即同步"<br>2. 观察状态提示 |
| 预期结果 | 显示"正在同步..."，完成后显示"同步完成"或错误信息 |
| 优先级 | P1 |

### TC-09: 网络错误处理
| 项目 | 内容 |
|------|------|
| 用例ID | TC-09 |
| 标题 | 无网络时同步失败提示 |
| 前置条件 | 用户已登录 |
| 测试步骤 | 1. 断开网络<br>2. 点击"立即同步" |
| 预期结果 | 显示"网络错误"或"同步失败"提示 |
| 优先级 | P2 |

### TC-10: 登录状态持久化
| 项目 | 内容 |
|------|------|
| 用例ID | TC-10 |
| 标题 | App 重启后保持登录状态 |
| 前置条件 | 用户已登录 |
| 测试步骤 | 1. 登录账号<br>2. 杀掉 App<br>3. 重新打开 App |
| 预期结果 | 自动恢复登录状态，无需重新登录 |
| 优先级 | P1 |

---

## API 测试命令

### 登录
```bash
curl -X POST https://csbaby-sync-server.onrender.com/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"123456"}'
```

### 查询云端数据
```bash
# 替换 <TOKEN> 和 <TENANT_ID>
curl https://csbaby-sync-server.onrender.com/sync/all?tenantId=<TENANT_ID> \
  -H "Authorization: Bearer <TOKEN>"
```

### 推送本地数据到云端
```bash
curl -X POST https://csbaby-sync-server.onrender.com/sync/push \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "tenantId": "<TENANT_ID>",
    "keywordRules": [...],
    "aiModelConfigs": [],
    "userStyleProfile": null,
    "appConfigs": [],
    "scenarios": [],
    "replyHistory": [],
    "messageBlacklist": [],
    "deletedIds": {},
    "baseVersion": 0
  }'
```

---

## 已知问题

| 问题 | 状态 | 说明 |
|------|------|------|
| 后端 sync/push 返回 500 | 待修复 | 后端处理逻辑需要调试 |
| 华为悬浮窗权限检测 | 已修复 | 使用 AppOpsManager 替代 Settings.canDrawOverlays |

---

## 测试通过标准
- [ ] TC-01 登录成功
- [ ] TC-04 手动同步成功
- [ ] TC-05 数据上传到云端
- [ ] TC-08 状态显示正确
- [ ] TC-10 登录状态持久化